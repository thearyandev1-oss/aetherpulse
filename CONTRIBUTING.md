# Contributing to AetherPulse
We welcome contributions! Please follow our enterprise standard guidelines:
1. Ensure `npm run lint` passes (ESLint + Prettier).
2. Ensure 100% test coverage via `npm run test --coverage`.
3. Adhere to our `SECURITY.md` and `CODE_OF_CONDUCT.md`.
